
file(GLOB SAMPLE_HEADER_FILES "${CMAKE_CURRENT_SOURCE_DIR}/*.h")
list(APPEND MAIN_HEADER_FILES ${SAMPLE_HEADER_FILES})

file(GLOB SAMPLE_SOURCE_FILES "${CMAKE_CURRENT_SOURCE_DIR}/*.cpp")
list(APPEND MAIN_SOURCE_FILES ${SAMPLE_SOURCE_FILES})

set(EXAMPLE_NAME helloGange)

add_executable(${EXAMPLE_NAME} ${MAIN_HEADER_FILES} ${MAIN_SOURCE_FILES})

target_link_libraries(${EXAMPLE_NAME} ${GANGE_CORE_LIBS})